# Basic example

Runnable example for the module:

```bash
terraform init
terraform plan
```

Requires a configured `helm` and `kubernetes` provider pointing at the target
cluster. Replace the placeholder secret values before applying.
