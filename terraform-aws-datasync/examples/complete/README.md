# Complete DataSync example

Provisions a scheduled DataSync task that copies from an S3-compatible
object-storage source (Google Cloud Storage) to an Amazon S3 bucket, with a
module-created S3 access role and a CloudWatch log group.

```bash
terraform init
terraform plan
```
