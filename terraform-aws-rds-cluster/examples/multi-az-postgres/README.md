# Multi-AZ DB cluster (PostgreSQL)

Multi-AZ DB cluster (`engine = "postgres"`) — three managed instances across AZs with semi-sync replication, gp3 storage with provisioned IOPS.

```bash
terraform init
terraform plan
terraform apply
```
